/**
 * Runtime configuration for OpenLabsX Frontend
 * This file can be modified after deployment to change settings
 * without rebuilding the application.
 */
// Set your API URL here - this can be modified after deployment without rebuilding
//
// Examples:
// 1. Use local proxy (handles CORS): "http://localhost:3001"
// 2. Direct API access (requires CORS): "https://api.openlabsx.com"
// 3. Same origin (if hosting API on same domain): ""
window.__API_URL__ = window.__API_URL__ || "http://localhost:3001";
